import turtle
from turtle import Turtle
class ball(Turtle):
    def __init__(self,radius,colour,dx,dy):
        Turtle.__init__ (self)
        Turtle.penup()
        self.colour=colour
        self.raduis=raduis
        self.dx=dx
        self.dy=dy
        self.shape=('circle')
        self.colour(colour) 
        self.shape(r/10)
    def move(self,screen_width,screen_height):
        self.current_x=xcor()
        self.move(x,y)
        self.new_x=xcor()
        self.current_y=ycor()
        self.move(x,y)
        self.new_y=ycor()
        right_side_ball=x+raduis
        
 
